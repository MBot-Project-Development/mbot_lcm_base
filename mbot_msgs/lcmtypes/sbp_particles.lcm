package mbot_lcm_msgs;

struct sbp_particles
{
    // number of particles = 50
    // horizon = 10
    // 10 * 6  = 60
    // can be set larger for flexibility (unsused dimension can be set to 0)
    double particles[50][60];
}